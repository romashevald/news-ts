import {Sort} from "../data/Sort";
import {NewsItem} from "../data/NewsItem";

export class NewsService {
    public getNews(pageNumber: number, limit: number, sort: Sort): NewsItem[] {
        return [];
    }
}